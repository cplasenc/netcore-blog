@model IEnumerable<Post>

<h2>Últimos artículos</h2>
<ul>
    @foreach(var post in Model)
    {
        <li>
            <h3>@post.Title</h3>
            <div>Creado por @post.Author el @post.Date.ToShortDateString()</div>
        </li>
    }
</ul>